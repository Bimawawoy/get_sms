<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight">
    
    <!-- Konten tombol dengan sudut yang membulat -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#FF6200EE"/> <!-- Warna tombol -->
            <corners android:radius="8dp"/> <!-- Sudut membulat -->
            <padding android:left="10dp" android:top="10dp"
                android:right="10dp" android:bottom="10dp"/>
        </shape>
    </item>

    <!-- Ripple mengikuti bentuk sudut -->
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF"/> <!-- Warna ripple -->
            <corners android:radius="8dp"/> <!-- Sudut membulat agar sesuai tombol -->
        </shape>
    </item>

</ripple>
